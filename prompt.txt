You are a senior frontend engineer building "Bhasini."
All previous components exist. Now build the AgentConversationPage.

REFERENCE TYPOGRAPHY (from ElevenLabs screenshot):
  - Thin, sharp, elegant — NOT bold heavy text
  - Large numbers/headings: Plus Jakarta Sans, weight 300-400
  - Body: DM Sans, weight 300-400, generous line-height
  - Labels: JetBrains Mono, tiny, spaced, uppercase
  - Everything breathes with whitespace
  - Color: mostly #0D0D0D on white, minimal color accents

════════════════════════════════════════════════════════════════
ALSO FIX: Agent Detail Modal (AgentDetailModal.tsx)
Make it more informative, stylish, premium — not static.
════════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PART A: REDESIGNED AgentDetailModal.tsx
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Completely replace AgentDetailModal.tsx with this design:

OVERLAY:
  position fixed, inset 0
  background rgba(13,13,13,0.60)
  backdrop-filter blur(12px) saturate(160%)
  z-index 400
  display flex, align-items center, justify-content center
  padding 24px

MODAL PANEL:
  background white
  border-radius 28px
  width 100%, max-width 580px
  max-height 88vh
  overflow-y auto
  overflow-x hidden
  position relative
  box-shadow:
    0 0 0 1px rgba(0,0,0,0.06),
    0 32px 80px rgba(0,0,0,0.18),
    0 8px 24px rgba(0,0,0,0.08)
  
  framer-motion:
    initial: opacity 0, scale 0.93, y 24
    animate: opacity 1, scale 1, y 0
    exit: opacity 0, scale 0.95, y 12
    transition: duration 0.4, ease [0.16,1,0.3,1]

MODAL HERO BAND (top section — gradient bg):
  background: linear-gradient(160deg,
    {agent.orbGradient[0]}14 0%,
    {agent.orbGradient[1]}08 50%,
    rgba(247,245,242,0) 100%)
  padding: 32px 32px 24px
  position: relative
  border-radius: 28px 28px 0 0

  CLOSE BUTTON:
    position absolute, top 20px, right 20px
    width 36px, height 36px
    border-radius 50%
    background rgba(0,0,0,0.06)
    border none, cursor pointer
    display flex, align-items center, justify-content center
    color #6B6B6B
    transition background 150ms
    hover: background rgba(0,0,0,0.10)
    <X size={16}/>

  ORB + NAME ROW:
    display flex, align-items center, gap 20px
    margin-bottom 20px

    ORB (large, 80px):
      width 80px, height 80px, border-radius 50%
      flex-shrink 0
      background: radial-gradient(circle at 32% 28%,
        {gradient stops from agent.orbGradient})
        Example: radial-gradient(circle at 32% 28%,
          {agent.orbGradient[0]}FF 0%,
          {agent.orbGradient[1]}FF 50%,
          {agent.orbGradient[2]}FF 100%)
      box-shadow:
        0 8px 32px {agent.orbGradient[0]}40,
        inset 0 1px 0 rgba(255,255,255,0.3)
      position relative
      
      Inner highlight:
        position absolute, top 12%, left 18%
        width 32%, height 26%
        border-radius 50%
        background radial-gradient(circle,
          rgba(255,255,255,0.6) 0%, transparent)
        filter blur(3px)
      
      framer-motion animate:
        y [0, -6, 0], scale [1, 1.03, 1]
        transition duration 4, repeat Infinity, ease easeInOut

    NAME + BADGE COLUMN:
      display flex, flex-direction column, gap 10px

      Agent name:
        font-family 'Plus Jakarta Sans'
        font-size: 28px
        font-weight: 300   ← THIN, elegant
        letter-spacing: -0.02em
        color: #0D0D0D
        line-height: 1

      Industry badge:
        display inline-flex, align-items center, gap 6px
        padding: 5px 12px
        background: rgba(0,0,0,0.05)
        border-radius: 9999px
        width: fit-content
        
        Colored dot:
          width 7px, height 7px, border-radius 50%
          background: {agent.orbGradient[0]}
        
        Text:
          font-family 'DM Sans'
          font-size: 12px, font-weight: 500
          color: #6B6B6B
          letter-spacing: 0.02em

  QUICK STATS ROW (3 stats inline):
    display grid, grid-template-columns repeat(3, 1fr)
    gap 1px
    background #F0EFED
    border-radius 14px
    overflow hidden
    
    Each stat cell:
      background white, padding 14px 16px
      display flex, flex-direction column, gap 3px
      
      Value:
        font-family 'Plus Jakarta Sans'
        font-size: 20px, font-weight: 600
        color: #0D0D0D, letter-spacing: -0.02em
      
      Label:
        font-family 'DM Sans'
        font-size: 10px, font-weight: 500
        color: #9E9E9E, letter-spacing: 0.06em
        text-transform: uppercase
    
    Stats per industry:
      health-medical: 
        {value:'< 1s', label:'Response'} | {value:'4.9★', label:'Rating'} | {value:'22k+', label:'Sessions'}
      tourism:
        {value:'3.2s', label:'Avg. Call'} | {value:'4.8★', label:'Rating'} | {value:'8k+', label:'Sessions'}
      customer-support:
        {value:'< 800ms', label:'Latency'} | {value:'4.7★', label:'CSAT'} | {value:'77k+', label:'Calls'}
      real-estate:
        {value:'< 1.2s', label:'Response'} | {value:'4.8★', label:'Rating'} | {value:'5k+', label:'Leads'}
      travel:
        {value:'< 1s', label:'Response'} | {value:'4.9★', label:'Rating'} | {value:'12k+', label:'Trips'}
      mental-health:
        {value:'< 1s', label:'Response'} | {value:'5.0★', label:'Rating'} | {value:'3k+', label:'Sessions'}

MODAL BODY SECTION:
  padding 0 32px 32px

  DESCRIPTION:
    margin-top 24px, margin-bottom 28px
    font-family 'DM Sans'
    font-size: 15px, font-weight: 300
    line-height: 1.75
    color: #4A4A4A
    letter-spacing: 0.005em

  DIVIDER LINE:
    height 1px, background #F0EFED, margin-bottom 28px

  SECTION: "SPEAKS"
    Label style (reuse for all section labels):
      font-family 'JetBrains Mono'
      font-size: 9px, font-weight: 500
      letter-spacing: 0.18em, text-transform: uppercase
      color: #9E9E9E, margin-bottom: 12px
    
    Language chips — display flex, gap 8px, flex-wrap wrap:
      Each chip:
        padding: 7px 16px
        border-radius: 9999px
        border: 1.5px solid {agent.orbGradient[0]}35
        background: {agent.orbGradient[0]}08
        
        font-family: 'DM Sans'
        font-size: 13px, font-weight: 500
        color: {agent.orbGradient[0]}
        
        framer-motion whileHover:
          scale 1.04
          background {agent.orbGradient[0]}15
          transition duration 0.15

  SECTION: "TRY ASKING" (margin-top 28px)
    Same label as above.
    
    Questions list — display flex, flex-direction column, gap 0:
      Each question item:
        display flex, align-items center, gap 14px
        padding: 14px 0
        border-bottom: 1px solid #F5F5F5
        cursor pointer
        
        transition background 150ms
        hover: background #FAFAFA
        border-radius 8px
        padding-left 8px, padding-right 8px
        
        Left icon circle:
          width 28px, height 28px, border-radius 50%
          background: {agent.orbGradient[0]}10
          border: 1px solid {agent.orbGradient[0]}20
          display flex, align-items center, justify-content center
          flex-shrink 0
          
          <MessageCircle size={13}/> 
          color: {agent.orbGradient[0]}
        
        Question text:
          font-family: 'DM Sans'
          font-size: 14px, font-weight: 400
          color: #2A2A2A, line-height: 1.4
        
        Right arrow (shows on hover):
          margin-left auto, flex-shrink 0
          <ArrowRight size={14}/> color #C8C5C0
          framer-motion: opacity 0 → 1 on parent hover

  SECTION: "CAPABILITIES" (margin-top 28px)
    Same label as above.
    
    Capabilities grid:
      display grid
      grid-template-columns repeat(2, 1fr)
      gap 8px
      margin-top 12px
      
      Each capability:
        display flex, align-items center, gap 10px
        padding: 10px 14px
        background: #FAFAFA
        border: 1px solid #F0EFED
        border-radius: 10px
        
        Check icon:
          width 18px, height 18px, border-radius 50%
          background: {agent.orbGradient[0]}12
          display flex, align-items center, justify-content center
          flex-shrink 0
          <Check size={10}/> color {agent.orbGradient[0]}
        
        Text:
          font-family 'DM Sans'
          font-size: 12px, font-weight: 500
          color: #4A4A4A

  SAMPLE CONVERSATION PREVIEW (new section, margin-top 28px):
    Label: "PREVIEW"
    
    A small mock conversation showing what this agent sounds like:
    Small rounded card, background #FAFAFA, border-radius 16px
    padding 16px, border 1px solid #F0EFED
    
    Based on agent.industry, show 2 exchanges:
    
    health-medical:
      User: "मुझे सिरदर्द है"
      Agent: "समझा। कितने समय से है? क्या आपने कोई दवा ली है?"
    
    tourism:
      User: "Hampi ke baare mein batao"
      Agent: "Hampi ek UNESCO site hai — 500+ temples, stunning ruins!"
    
    customer-support:
      User: "Mujhe refund chahiye"
      Agent: "ज़रूर। Order number share karein, main check karta hoon."
    
    real-estate:
      User: "2BHK ka rate kya hai?"
      Agent: "Noida sector 62 mein ₹65-75 lakh range hai abhi."
    
    travel:
      User: "Ladakh trip plan karo"
      Agent: "10 din ka plan: Day 1-2 Leh, Day 3-4 Pangong..."
    
    mental-health:
      User: "Main bahut stressed hoon"
      Agent: "Sunita hoon main. Aap safe hain. Kya share karna chahenge?"
    
    Each message row:
      display flex, gap 8px, margin-bottom 10px
      
      For user message (right-aligned):
        justify-content flex-end
        Bubble: background #0D0D0D, color white
          padding 8px 14px, border-radius 16px 16px 4px 16px
          font-size 12px, font-family 'DM Sans', font-weight 400
      
      For agent message (left-aligned):
        Colored dot (6px, agent gradient color[0])
        Bubble: background white, border 1px solid #E8E8E8
          padding 8px 14px, border-radius 16px 16px 16px 4px
          font-size 12px, font-family 'DM Sans', font-weight 400
          color #2A2A2A
    
    Animate-in on mount:
      stagger each message 200ms delay
      each: initial opacity 0, y 8
        animate opacity 1, y 0
        transition duration 0.4

MODAL FOOTER:
  padding 20px 32px 32px
  border-top 1px solid #F5F5F5
  display flex, flex-direction column, gap 12px

  PRIMARY BUTTON — "Start Conversation":
    width 100%, height 52px
    background #0D0D0D
    border none, border-radius 9999px
    display flex, align-items center, justify-content center, gap 10px
    cursor pointer
    
    LEFT: Pulsing green dot:
      width 8px, height 8px, border-radius 50%
      background #22C55E
      framer-motion animate: scale [1,1.4,1], opacity [1,0.5,1]
        transition duration 1.8, repeat Infinity
    
    TEXT: "Start Conversation"
      font-family 'Plus Jakarta Sans'
      font-size: 15px, font-weight: 500
      color: white, letter-spacing: -0.01em
    
    RIGHT: <Phone size={15}/> color white
    
    framer-motion whileHover: scale 1.01, backgroundColor '#1A1A1A'
    whileTap: scale 0.99
    
    onClick: onClose() then navigate to /agents/:id/talk

  SECONDARY ROW — two buttons side by side:
    display flex, gap 8px
    
    "View Docs" button:
      flex 1, height 44px
      background transparent
      border 1.5px solid #E0DED9
      border-radius 9999px
      font-size 14px, font-weight 400
      font-family 'DM Sans', color #6B6B6B
      cursor pointer
      hover: border-color #C8C5C0, color #0D0D0D
    
    "Share" button:
      width 44px, height 44px
      border 1.5px solid #E0DED9
      border-radius 50%
      background transparent
      cursor pointer
      display flex, align-items center, justify-content center
      <Share2 size={15}/> color #6B6B6B
      hover: border-color #C8C5C0

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PART B: AgentConversationPage — FULL SCREEN
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

File: src/pages/AgentConversationPage/index.tsx

This is a FULL SCREEN experience — NO navbar visible.
Route: /agents/:agentId/talk

STATE MACHINE:
  type CallState = 'idle' | 'connecting' | 'active' | 'ended'
  
  const { agentId } = useParams()
  const navigate = useNavigate()
  const agent = agents.find(a => a.id === agentId)
  
  const [callState, setCallState] = useState<CallState>('idle')
  const [activeOrb, setActiveOrb] = useState<'user'|'agent'|null>(null)
  const [isMuted, setIsMuted] = useState(false)
  const [selectedLanguage, setSelectedLanguage] = useState(
    agent?.languages[0] || 'Hindi'
  )
  const [langDropOpen, setLangDropOpen] = useState(false)
  const [transcript, setTranscript] = useState<TranscriptMessage[]>([])
  const [sessionTime, setSessionTime] = useState(0)
  const transcriptRef = useRef<HTMLDivElement>(null)

  interface TranscriptMessage {
    id: number
    speaker: 'user' | 'agent'
    text: string
    timestamp: number
    isFinal: boolean
  }

SIMULATED CONVERSATION (for demo — real SDK added later):
  When callState becomes 'active', start automated demo:
  
  const demoScript based on agent.industry:
    health-medical: [
      {speaker:'user', text:'Doctor sahab, mujhe teen dino se bukhaar hai', delay:2000, duration:3000},
      {speaker:'agent', text:'Samjha. Kitna temperature hai? Kya aapne paracetamol li hai?', delay:1000, duration:3500, isTyping:true},
      {speaker:'user', text:'102 degree hai. Ek tablet li thi subah.', delay:800, duration:2500},
      {speaker:'agent', text:'102 pe concern hona chahiye. Kya body mein dard hai ya throat mein kuch feel ho raha hai?', delay:1000, duration:4000, isTyping:true},
    ]
    
    customer-support: [
      {speaker:'user', text:'Mujhe refund chahiye mere order ka', delay:2000, duration:2800},
      {speaker:'agent', text:'Zaroor main madad karunga. Kripya apna order number share karein.', delay:1000, duration:3200, isTyping:true},
      {speaker:'user', text:'Order number hai EL4543490', delay:800, duration:2000},
      {speaker:'agent', text:'Dhanyavaad. Maine aapka order dekha — refund process shuru kar diya hai. 3-5 din mein credit ho jayega.', delay:1200, duration:4500, isTyping:true},
    ]
    
    travel: [
      {speaker:'user', text:'Mujhe Ladakh jaana hai November mein', delay:2000, duration:3000},
      {speaker:'agent', text:'Arrey November mein Ladakh bohot cold hoga — minus 20 degree! Main suggest karunga October ya June-September.', delay:1000, duration:4000, isTyping:true},
      {speaker:'user', text:'Toh October mein 7 days ka plan banao', delay:800, duration:2500},
      {speaker:'agent', text:'Perfect! Day 1-2: Leh acclimatization. Day 3: Nubra Valley by Khardung La. Day 4-5: Pangong Tso...', delay:1200, duration:5000, isTyping:true},
    ]
    
    Default for all others:
    [
      {speaker:'user', text:'Namaste, main aapki help chahta hoon', delay:2000, duration:2500},
      {speaker:'agent', text:'Namaste! Main yahan hoon aapki poori madad karne ke liye. Kya chahiye aapko?', delay:1000, duration:3500, isTyping:true},
      {speaker:'user', text:'Mujhe kuch important jaankari chahiye', delay:800, duration:2500},
      {speaker:'agent', text:'Zaroor! Batayein kya jaanna chahte hain — main apni poori koshish karunga.', delay:1000, duration:3500, isTyping:true},
    ]
  
  Run demo loop same as InlineOrbDemo — loop after completion.

SESSION TIMER (when callState === 'active'):
  useEffect interval, increments every second
  Display as MM:SS format

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PAGE LAYOUT — THREE PANELS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Full screen: width 100vw, height 100vh
Background: #F7F5F2
Display: flex
overflow: hidden

PANEL 1 — LEFT SIDEBAR (280px wide, fixed):
  height 100vh
  background white
  border-right 1px solid #F0EFED
  display flex, flex-direction column
  flex-shrink 0
  
  SIDEBAR HEADER (64px):
    padding 0 20px
    display flex, align-items center, justify-content space-between
    border-bottom 1px solid #F0EFED
    
    Left: Back button:
      display flex, align-items center, gap 8px
      cursor pointer
      onClick: navigate('/agents')
      
      <ArrowLeft size={16}/> color #6B6B6B
      Text: "Agents"
        font-family 'DM Sans', font-size 13px
        font-weight 500, color #6B6B6B
        hover: color #0D0D0D
    
    Right: Session timer (only when active):
      font-family 'JetBrains Mono', font-size 13px
      color #FF6B35, font-weight 500
      text: formatTime(sessionTime) e.g. "02:34"
      
      AnimatePresence — only renders when callState==='active'

  AGENT INFO SECTION:
    padding 24px 20px
    border-bottom 1px solid #F0EFED
    
    Small orb (48px) + name + status:
      display flex, align-items center, gap 14px
      margin-bottom 16px
      
      Orb: 48px × 48px, same gradient as agent
        float animation y [0,-4,0] duration 3.5 repeat Infinity
      
      Right column:
        Agent name:
          font-family 'Plus Jakarta Sans'
          font-size 16px, font-weight 400
          color #0D0D0D, letter-spacing -0.01em
        
        Status (changes with callState):
          display flex, align-items center, gap 5px
          font-family 'DM Sans', font-size 11px, color #9E9E9E
          
          'idle': gray dot + "Ready"
          'connecting': yellow pulsing dot + "Connecting..."
          'active': green pulsing dot + "Live"
          'ended': gray dot + "Ended"
    
    Industry + language badges:
      display flex, gap 6px, flex-wrap wrap
      
      Industry badge:
        font-size 10px, padding 3px 10px
        background #EFEFED, border-radius 9999px
        color #6B6B6B, font-family 'DM Sans', font-weight 500
      
      Language badge (active language):
        font-size 10px, padding 3px 10px
        background rgba(255,107,53,0.08)
        border 1px solid rgba(255,107,53,0.20)
        border-radius 9999px
        color #FF6B35, font-family 'DM Sans', font-weight 600

  LANGUAGE SELECTOR:
    padding 16px 20px
    border-bottom 1px solid #F0EFED
    
    Label: "LANGUAGE"
      font-family 'JetBrains Mono', font-size 9px
      letter-spacing 0.16em, color #9E9E9E
      margin-bottom 10px
    
    Language buttons (one per agent language):
      display flex, flex-direction column, gap 4px
      
      Each:
        padding 9px 12px, border-radius 10px
        cursor pointer, display flex, align-items center, gap 10px
        transition background 150ms
        
        Active language:
          background rgba(255,107,53,0.08)
          border 1px solid rgba(255,107,53,0.15)
        
        Inactive:
          background transparent
          border 1px solid transparent
          hover: background #F5F5F5
        
        Left: flag emoji (🇮🇳 for all Indian langs)
        
        Language name:
          font-family 'DM Sans', font-size 13px
          font-weight: 500 when active, 400 when not
          color: #FF6B35 when active, #4A4A4A when not
        
        Right check (active only):
          <Check size={12}/> color #FF6B35

  TRANSCRIPT SECTION (flex 1, scrollable):
    flex 1, overflow-y auto
    padding 16px 20px
    
    Label: "TRANSCRIPT"
      font-family 'JetBrains Mono', font-size 9px
      letter-spacing 0.16em, color #9E9E9E
      margin-bottom 14px
      position sticky, top 0
      background white, padding-bottom 8px
      z-index 1
    
    When no messages:
      Centered placeholder:
        font-family 'DM Sans', font-size 13px
        color #C8C5C0, text-align center, margin-top 24px
        Text: "Transcript will appear here\nduring your conversation"
        white-space pre-line
    
    Message list (AnimatePresence):
      display flex, flex-direction column, gap 12px
      
      Each message — motion.div:
        key={message.id}
        initial: opacity 0, y 8
        animate: opacity 1, y 0
        transition: duration 0.3, ease [0.16,1,0.3,1]
        
        Speaker label:
          font-family 'JetBrains Mono', font-size 8px
          font-weight 500, letter-spacing 0.14em
          text-transform uppercase
          color: rgba(255,107,53,0.7) for user
          color: rgba(107,107,107,0.6) for agent
          margin-bottom 3px
        
        Message text:
          font-family 'DM Sans'
          font-size: 12px, font-weight: 400
          line-height: 1.55, color: #2A2A2A
          
          NOT final (streaming):
            color #9E9E9E, font-style italic
          Final: full opacity
        
        Timestamp:
          font-family 'JetBrains Mono', font-size 8px
          color #C8C5C0, margin-top 2px
          text: formatTime(message.timestamp)
    
    Auto-scroll: useEffect when transcript changes:
      transcriptRef.current?.scrollTo({
        top: transcriptRef.current.scrollHeight,
        behavior: 'smooth'
      })

PANEL 2 — CENTER STAGE (flex 1):
  height 100vh
  display flex, flex-direction column
  align-items center, justify-content center
  position relative
  overflow hidden
  
  Background per callState:
    'idle': #F7F5F2 (plain warm white)
    'connecting': subtle radial pulse animation
    'active': radial gradients from both orbs bleeding:
      radial-gradient(ellipse 60% 50% at 35% 50%,
        {agent.orbGradient[0]}08 0%, transparent 60%)
      + radial-gradient(ellipse 60% 50% at 65% 50%,
        rgba(60,207,207,0.06) 0%, transparent 60%)
    'ended': #F7F5F2
  
  Transition: background 1s ease

  TOP STATUS BAR:
    position absolute, top 24px, left 0, right 0
    display flex, align-items center, justify-content center
    
    AnimatePresence based on callState:
      'idle':
        Pill: "Press to start conversation"
          background rgba(0,0,0,0.05)
          border 1px solid rgba(0,0,0,0.08)
          border-radius 9999px, padding 8px 20px
          font-family 'DM Sans', font-size 13px
          color #6B6B6B
      
      'connecting':
        Pill: animated
          background rgba(255,165,0,0.08)
          border 1px solid rgba(255,165,0,0.20)
          display flex, align-items center, gap 8px
          
          Spinner (border animation):
            width 12px, height 12px, border-radius 50%
            border 1.5px solid rgba(255,165,0,0.3)
            border-top-color #F59E0B
            animation spin 0.8s linear infinite
          
          Text: "Connecting to {agent.name}..."
            font-family 'DM Sans', font-size 13px, color #F59E0B
      
      'active':
        Pill: live indicator
          background rgba(34,197,94,0.08)
          border 1px solid rgba(34,197,94,0.20)
          display flex, align-items center, gap 8px
          
          Pulsing green dot:
            framer-motion opacity [1,0.3,1] duration 1.4 repeat Infinity
          
          Text: "{formatTime(sessionTime)} · Live"
            font-family 'JetBrains Mono', font-size 12px
            color #22C55E, font-weight 500
      
      'ended':
        Pill: "Conversation ended"
          background rgba(0,0,0,0.05)
          Text color #6B6B6B

  MAIN ORB STAGE:
    display flex, align-items center, justify-content center
    gap: varies by state
    width 100%, position relative
    
    'idle' state — SINGLE orb (agent), centered:
      Agent orb: 200px × 200px
      Floating animation: y [0,-16,0] scale [1,1.03,1]
        duration 5 repeat Infinity
      
      Below orb: agent name in thin typography
        font-family 'Plus Jakarta Sans'
        font-size 22px, font-weight 300
        color #0D0D0D, letter-spacing -0.02em
        margin-top 24px
      
      Below name: "Tap to begin"
        font-family 'DM Sans', font-size 14px
        color #9E9E9E, margin-top 8px

    'connecting' state — orb pulses with rings:
      Same single orb but:
      3 concentric expanding rings behind it:
        Each ring: border 1px solid {agent gradient color}20
        border-radius 50%
        framer-motion animate:
          scale [1, 2.5, 1], opacity [0.5, 0, 0]
          transition: duration 2, repeat Infinity
          delay: 0, 0.5, 1.0 for three rings
    
    'active' state — TWO ORBS facing each other:
      Layout: flex row, align-items center, gap 60px
      
      LEFT — USER ORB (140px):
        background radial-gradient(circle at 32% 28%,
          #FFD4A0 0%, #FF9A3C 22%, #FF6B35 52%,
          #C44A1A 80%, #8B2500 100%)
        box-shadow as before
        
        Speaking state animations:
          when activeOrb==='user': orbSpeaking (scale pulse)
          when activeOrb==='agent': breathe gently
          
        Speaking ring when active (same as InlineOrbDemo)
        
        Label below: "YOU"
          font-family 'JetBrains Mono', font-size 9px
          letter-spacing 0.18em, color #9E9E9E
      
      CENTER — PARTICLE CANVAS (160px × 240px):
        Same particle system as InlineOrbDemo
        But larger canvas
        
        PLUS: when neither orb is speaking (between turns),
        show a thin pulsing horizontal line instead of particles:
          position absolute, left 0, right 0
          top 50%, height 1px
          background linear-gradient(90deg,
            transparent, rgba(255,107,53,0.3),
            rgba(60,207,207,0.3), transparent)
          framer-motion: opacity [0.3,0.8,0.3]
            transition: duration 2, repeat Infinity
      
      RIGHT — AGENT ORB (140px):
        Same gradient as InlineOrbDemo right orb
        Same speaking/listening states (inverted)
        
        Label below: "BHASINI"
          font-family 'JetBrains Mono', font-size 9px
          letter-spacing 0.18em, color #9E9E9E
      
      Entrance animation for two-orb layout:
        User orb: initial x -80, opacity 0
          animate x 0, opacity 1
          transition: duration 0.6, ease [0.16,1,0.3,1]
        Agent orb: initial x 80, opacity 0
          animate x 0, opacity 1
          same transition
    
    'ended' state:
      Single agent orb, smaller (120px), opacity 0.5
      Text: "Conversation ended"
        font-family 'DM Sans', font-size 16px, color #9E9E9E
        margin-top 20px
      
      Button: "Start New"
        height 44px, padding 0 24px
        background #0D0D0D, color white
        border-radius 9999px, cursor pointer
        font-family 'DM Sans', font-size 14px, font-weight 500
        margin-top 16px
        onClick: setCallState('idle'), setTranscript([]),
          setSessionTime(0)

  CURRENT SPEAKER TEXT (shows during active call):
    position absolute, bottom 140px, left 0, right 0
    display flex, justify-content center
    pointer-events none
    
    AnimatePresence mode="wait":
      When there is a current streaming message:
        motion.div:
          initial: opacity 0, y 12
          animate: opacity 1, y 0
          exit: opacity 0, y -8
          transition: duration 0.35
          
          Max-width 480px, text-align center
          
          Speaker label:
            font-family 'JetBrains Mono', font-size 9px
            letter-spacing 0.16em, uppercase
            color: #FF6B35 for user, #6B6B6B for agent
            margin-bottom 6px
          
          Text:
            font-family 'DM Sans'
            font-size: 18px, font-weight: 300
            color: #0D0D0D, line-height: 1.5
            letter-spacing: 0.005em
            
            ← THIS IS THE KEY TYPOGRAPHY MOMENT
               Thin, elegant, readable, like ElevenLabs

  BOTTOM CONTROLS:
    position absolute, bottom 0, left 0, right 0
    height 100px
    display flex, align-items center, justify-content center
    gap 16px
    background linear-gradient(to top,
      rgba(247,245,242,0.95) 0%, transparent 100%)
    padding-bottom 24px
    
    Layout: three controls centered
    
    LEFT CONTROL — Mute button:
      width 52px, height 52px, border-radius 50%
      background: when muted rgba(239,68,68,0.10) else white
      border: when muted 1.5px solid rgba(239,68,68,0.25) else 1.5px solid #E0DED9
      cursor pointer
      display flex, align-items center, justify-content center
      box-shadow 0 2px 8px rgba(0,0,0,0.06)
      transition all 200ms
      
      When muted: <MicOff size={20}/> color #EF4444
      When not: <Mic size={20}/> color #4A4A4A
      
      onClick: setIsMuted(m => !m)
      
      framer-motion whileHover: scale 1.06
      whileTap: scale 0.94

    CENTER CONTROL — Main call button (LARGE):
      width 72px, height 72px, border-radius 50%
      cursor pointer
      display flex, align-items center, justify-content center
      border none
      position relative
      
      When callState === 'idle':
        background #0D0D0D
        box-shadow 0 8px 32px rgba(0,0,0,0.20)
        <Phone size={26}/> color white
        
        onClick: async start sequence:
          setCallState('connecting')
          await delay(2000)
          setCallState('active')
          startDemoConversation()
        
        framer-motion whileHover: scale 1.08
        whileTap: scale 0.96
      
      When callState === 'connecting':
        background #F59E0B
        Spinner overlay: rotating border
        disabled
      
      When callState === 'active':
        background linear-gradient(135deg, #EF4444, #DC2626)
        box-shadow 0 8px 32px rgba(239,68,68,0.35)
        <PhoneOff size={24}/> color white
        
        Pulsing ring behind button:
          position absolute, inset -8px, border-radius 50%
          border 2px solid rgba(239,68,68,0.25)
          framer-motion: scale [1,1.3,1], opacity [0.5,0,0.5]
            transition duration 1.8, repeat Infinity
        
        onClick:
          setCallState('ended')
          setActiveOrb(null)
          stopDemoConversation()
      
      When callState === 'ended':
        Same as idle

    RIGHT CONTROL — Language button:
      width 52px, height 52px, border-radius 50%
      background white
      border 1.5px solid #E0DED9
      cursor pointer
      display flex, align-items center, justify-content center
      box-shadow 0 2px 8px rgba(0,0,0,0.06)
      position relative
      
      Content: "🇮🇳" emoji font-size 18px
      
      onClick: setLangDropOpen(true)
      
      Language dropdown (same style as other dropdowns):
        position absolute, bottom calc(100%+8px), right 0
        min-width 160px
        
        Each language option: same styling as sidebar language buttons
        
        On select: setSelectedLanguage(lang), setLangDropOpen(false)
      
      framer-motion whileHover: scale 1.06
      whileTap: scale 0.94
    
    Below controls, centered:
      When active:
        Text: "Tap red button to end"
          font-family 'DM Sans', font-size 11px
          color #C8C5C0, margin-top 8px

PANEL 3 — RIGHT PANEL (320px, visible only on lg screens):
  height 100vh
  background white
  border-left 1px solid #F0EFED
  display flex, flex-direction column
  flex-shrink 0
  
  Hide below 1200px: display none
  
  HEADER (64px):
    padding 0 20px
    display flex, align-items center
    border-bottom 1px solid #F0EFED
    
    Text: "Session Info"
      font-family 'Plus Jakarta Sans'
      font-size 14px, font-weight 400
      color #0D0D0D, letter-spacing -0.01em

  LIVE METRICS (padding 20px):
    Label: "LIVE METRICS"
    
    4 metric rows:
      {label: 'Latency', value: '<800ms', color: '#22C55E'}
      {label: 'Language', value: selectedLanguage, color: '#FF6B35'}
      {label: 'Model', value: 'Bhasini Flash', color: '#1A73E8'}
      {label: 'Messages', value: transcript.length, color: '#6B6B6B'}
    
    Each row:
      display flex, align-items center, justify-content space-between
      padding 12px 0
      border-bottom 1px solid #F5F5F5
      
      Label: font-size 12px, color #9E9E9E, font-family 'DM Sans'
      
      Value: font-size 13px, font-weight 600
        color: row.color, font-family 'DM Sans'
    
    Latency value animates when active:
      Randomly cycles between 750ms, 820ms, 680ms, 790ms
      every 3 seconds using setInterval

  AGENT DETAILS (padding 20px, border-top 1px solid #F5F5F5):
    Label: "ABOUT THIS AGENT"
    
    System prompt preview:
      background #FAFAFA, border-radius 12px, padding 14px
      font-family 'JetBrains Mono', font-size 10px
      color #6B6B6B, line-height 1.6
      max-height 120px, overflow hidden
      position relative
      
      Text: first 200 chars of agent.systemPrompt + "..."
      
      Bottom gradient fade:
        position absolute, bottom 0, left 0, right 0
        height 32px
        background linear-gradient(to top,
          #FAFAFA 0%, transparent 100%)
    
    Agent languages:
      margin-top 16px
      Same chip styling as modal

  KEYBOARD SHORTCUTS (margin-top auto, padding 20px):
    border-top 1px solid #F5F5F5
    
    Label: "SHORTCUTS"
    
    Rows:
      {key: 'Space', action: 'Push to talk'}
      {key: 'M', action: 'Toggle mute'}
      {key: 'Esc', action: 'End call'}
    
    Each row:
      display flex, align-items center, justify-content space-between
      padding 8px 0
      
      Key: keyboard-style box:
        padding 2px 8px
        background #F0EFED, border-radius 6px
        border 1px solid #E0DED9
        font-family 'JetBrains Mono', font-size 10px
        color #4A4A4A
      
      Action: font-size 12px, color #9E9E9E, font-family 'DM Sans'
    
    Keyboard shortcut handler (useEffect):
      Space → push-to-talk (visual only)
      M → toggleMute()
      Escape → if active, end call

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
COMPLETE IMPORTS:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

AgentConversationPage/index.tsx:
import React, { useState, useEffect, useRef, useCallback } from 'react'
import { useParams, useNavigate } from 'react-router-dom'
import { motion, AnimatePresence } from 'framer-motion'
import {
  ArrowLeft, Phone, PhoneOff, Mic, MicOff,
  Check, Globe
} from 'lucide-react'
import { agents } from '../../data/agents'

AgentDetailModal.tsx:
import React, { useEffect, useRef } from 'react'
import { motion, AnimatePresence } from 'framer-motion'
import {
  X, Phone, MessageCircle, Check,
  ArrowRight, Share2
} from 'lucide-react'
import type { BhasiniAgent } from '../../data/agents'

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
HELPER FUNCTIONS:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

function formatTime(seconds: number): string {
  const m = Math.floor(seconds / 60).toString().padStart(2, '0')
  const s = (seconds % 60).toString().padStart(2, '0')
  return `${m}:${s}`
}

function buildOrbGradient(colors: string[]): string {
  return `radial-gradient(circle at 32% 28%, 
    ${colors[0]} 0%, ${colors[1]} 45%, ${colors[2]} 100%)`
}

function getCapabilities(industry: string): string[] {
  const map: Record<string, string[]> = {
    'health-medical': ['Symptom Check','Appointment Booking',
      'Report Explain','Doctor Finder','Prescription Help','Emergency Guide'],
    'tourism': ['Heritage Sites','Local Cuisine','Festival Guide',
      'Transport Tips','Hotel Suggest','Cultural Context'],
    'customer-support': ['Refund Processing','Order Tracking',
      'Complaint Filing','Account Help','Product Info','Escalation'],
    'real-estate': ['Property Search','Price Analysis','Document Guide',
      'RERA Check','Loan Advice','Locality Info'],
    'travel': ['Trip Planning','Hotel Booking','Itinerary Build',
      'Budget Planning','Visa Guide','Local Tips'],
    'mental-health': ['Active Listening','Breathing Exercises',
      'Mood Tracking','Grounding Techniques','Journal Prompts','Crisis Resources'],
  }
  return map[industry] || []
}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ACCEPTANCE CRITERIA:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

MODAL:
✓ Agent name uses font-weight 300 (thin, elegant)
✓ Quick stats row shows 3 metrics based on industry
✓ Description is light weight 300, generous line-height
✓ Language chips use agent's gradient color for border/bg
✓ Sample questions show hover arrow, clickable rows
✓ Capabilities show 2-column grid with check icons
✓ Mini conversation preview animates in staggered
✓ "Start Conversation" button has pulsing green dot
✓ Modal backdrop is blur+dark, not just dark

CONVERSATION PAGE:
✓ /agents/:agentId/talk renders full screen (no navbar)
✓ Three panels: left sidebar, center stage, right panel
✓ Center shows single agent orb in idle state with "Tap to begin"
✓ Phone button click → connecting state (spinner, yellow)
✓ After 2s → active state, two orbs appear from sides
✓ User orb: warm orange, agent orb: teal-blue
✓ Speaking orb pulses, listening orb breathes gently
✓ Particles travel between orbs
✓ Current transcript line shows below orbs, thin typography
✓ Session timer counts up in sidebar (MM:SS, saffron colored)
✓ Transcript messages accumulate in left sidebar
✓ Red phone button ends call → ended state
✓ Language switcher updates selectedLanguage
✓ Right panel shows live metrics, system prompt, shortcuts
✓ Keyboard shortcuts (M, Esc) work
✓ Mobile: right panel hidden, sidebar collapses
✓ Zero TypeScript errors
✓ Conversation script loops correctly