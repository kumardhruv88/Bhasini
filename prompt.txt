You are a senior frontend engineer at a funded Indian AI startup building "Bhasini" — 
a production-grade multilingual voice agent platform (think ElevenLabs but for India).
Stack: React 18 + Vite + TypeScript + Tailwind CSS + Framer Motion.

This is Prompt 1 of a long build sequence. Do exactly what is specified, nothing more, 
nothing less. No placeholder comments. Every file must be complete and runnable.

════════════════════════════════════════════════════════════
TASK: Build the complete global foundation — styles, tokens, 
app shell, routing, and utilities.
════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FILE 1: src/styles/globals.css
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Step 1: Google Fonts import at the very top:
@import url('https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:ital,opsz,wght@0,6..18,200..800;1,6..18,200..800&family=DM+Sans:ital,opsz,wght@0,9..40,100..1000;1,9..40,100..1000&family=JetBrains+Mono:wght@400;500&display=swap');

Step 2: :root block — ALL design tokens as CSS custom properties:

  /* Color — Base */
  --color-bg: #F7F5F2;
  --color-bg-dark: #0D0D0D;
  --color-text: #0D0D0D;
  --color-text-muted: #6B6B6B;
  --color-text-subtle: #9E9E9E;

  /* Color — Surfaces */
  --color-card: #EFEFED;
  --color-card-hover: #E8E6E3;
  --color-border: #E0DED9;
  --color-border-strong: #C8C5C0;
  --color-surface: #FFFFFF;

  /* Color — Accent (Saffron) */
  --color-accent: #FF6B35;
  --color-accent-light: #FF8F5E;
  --color-accent-bg: #FFF2EC;
  --color-accent-hover: #E85A25;

  /* Color — Blue (Google integration signal) */
  --color-blue: #1A73E8;
  --color-blue-light: #4D90F0;
  --color-blue-bg: #EBF3FE;

  /* Color — Semantic */
  --color-success: #22C55E;
  --color-warning: #F59E0B;
  --color-error: #EF4444;

  /* Color — Orb Gradients (per agent category) */
  --orb-medical: linear-gradient(135deg, #FF6B9D, #C44AFF, #7B2FFF);
  --orb-tourism: linear-gradient(135deg, #FF6B35, #FF9A3C, #FFC53D);
  --orb-support: linear-gradient(135deg, #3CCFCF, #1A73E8, #7B2FFF);
  --orb-realestate: linear-gradient(135deg, #22C55E, #1A73E8, #0EA5E9);
  --orb-travel: linear-gradient(135deg, #FF6B35, #FF3CAC, #784BA0);
  --orb-wellness: linear-gradient(135deg, #6366F1, #8B5CF6, #C084FC);
  --orb-neutral: linear-gradient(135deg, #D4D0CA, #EFEFED, #C8C5C0);
  --orb-hero: linear-gradient(135deg, #FF6B35 0%, #FF3CAC 40%, #784BA0 70%, #1A73E8 100%);

  /* Typography */
  --font-display: 'Plus Jakarta Sans', sans-serif;
  --font-body: 'DM Sans', sans-serif;
  --font-mono: 'JetBrains Mono', monospace;

  /* Font Sizes */
  --text-xs: 11px;
  --text-sm: 13px;
  --text-base: 15px;
  --text-md: 17px;
  --text-lg: 19px;
  --text-xl: 22px;
  --text-2xl: 28px;
  --text-3xl: 36px;
  --text-4xl: 48px;
  --text-5xl: 62px;
  --text-hero: 76px;

  /* Font Weights */
  --weight-regular: 400;
  --weight-medium: 500;
  --weight-semibold: 600;
  --weight-bold: 700;
  --weight-extrabold: 800;

  /* Line Heights */
  --leading-tight: 1.1;
  --leading-snug: 1.25;
  --leading-normal: 1.5;
  --leading-relaxed: 1.65;

  /* Letter Spacing */
  --tracking-tight: -0.03em;
  --tracking-snug: -0.02em;
  --tracking-normal: -0.01em;
  --tracking-wide: 0.04em;
  --tracking-wider: 0.08em;

  /* Spacing scale */
  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-5: 20px;
  --space-6: 24px;
  --space-8: 32px;
  --space-10: 40px;
  --space-12: 48px;
  --space-16: 64px;
  --space-20: 80px;
  --space-24: 96px;
  --space-32: 128px;

  /* Border Radii */
  --radius-sm: 6px;
  --radius-md: 10px;
  --radius-lg: 14px;
  --radius-xl: 20px;
  --radius-2xl: 28px;
  --radius-3xl: 40px;
  --radius-pill: 9999px;

  /* Shadows */
  --shadow-xs: 0 1px 2px rgba(0,0,0,0.05);
  --shadow-sm: 0 1px 4px rgba(0,0,0,0.06), 0 1px 2px rgba(0,0,0,0.04);
  --shadow-md: 0 4px 16px rgba(0,0,0,0.08);
  --shadow-lg: 0 12px 40px rgba(0,0,0,0.10);
  --shadow-xl: 0 24px 64px rgba(0,0,0,0.12);
  --shadow-card: 0 2px 12px rgba(0,0,0,0.05);
  --shadow-orb-accent: 0 32px 80px rgba(255,107,53,0.30);
  --shadow-orb-blue: 0 32px 80px rgba(26,115,232,0.25);
  --shadow-orb-purple: 0 32px 80px rgba(139,92,246,0.28);
  --shadow-focus: 0 0 0 3px rgba(255,107,53,0.30);

  /* Z-Index Scale */
  --z-behind: -1;
  --z-base: 0;
  --z-raised: 10;
  --z-dropdown: 100;
  --z-sticky: 200;
  --z-overlay: 300;
  --z-modal: 400;
  --z-toast: 500;

  /* Transitions */
  --ease-spring: cubic-bezier(0.16, 1, 0.3, 1);
  --ease-out: cubic-bezier(0, 0, 0.2, 1);
  --ease-in: cubic-bezier(0.4, 0, 1, 1);
  --duration-fast: 120ms;
  --duration-normal: 220ms;
  --duration-slow: 380ms;
  --duration-slower: 550ms;

  /* Layout */
  --max-width: 1280px;
  --nav-height: 64px;
  --sidebar-width: 240px;

Step 3: CSS Reset block:
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
html { 
  scroll-behavior: smooth; 
  font-size: 16px;
  -webkit-text-size-adjust: 100%;
}
body {
  font-family: var(--font-body);
  background-color: var(--color-bg);
  color: var(--color-text);
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  text-rendering: optimizeLegibility;
  min-height: 100vh;
  line-height: var(--leading-normal);
}
img, video, canvas, svg { display: block; max-width: 100%; }
input, button, textarea, select { font: inherit; }
p, h1, h2, h3, h4, h5, h6 { overflow-wrap: break-word; }
#root { isolation: isolate; }

Step 4: Custom scrollbar (webkit):
::-webkit-scrollbar { width: 5px; height: 5px; }
::-webkit-scrollbar-track { background: transparent; }
::-webkit-scrollbar-thumb { 
  background: var(--color-border-strong); 
  border-radius: var(--radius-pill); 
}
::-webkit-scrollbar-thumb:hover { background: var(--color-text-subtle); }

Step 5: Selection highlight:
::selection { background: var(--color-accent); color: #FFFFFF; }

Step 6: Focus visible ring (keyboard only):
:focus-visible {
  outline: none;
  box-shadow: var(--shadow-focus);
  border-radius: var(--radius-sm);
}

Step 7: Typography utility classes:
.font-display { font-family: var(--font-display); }
.font-body { font-family: var(--font-body); }
.font-mono { font-family: var(--font-mono); }

.text-hero {
  font-family: var(--font-display);
  font-size: var(--text-hero);
  font-weight: var(--weight-extrabold);
  line-height: var(--leading-tight);
  letter-spacing: var(--tracking-tight);
}
.text-display {
  font-family: var(--font-display);
  font-size: var(--text-5xl);
  font-weight: var(--weight-extrabold);
  line-height: var(--leading-tight);
  letter-spacing: var(--tracking-snug);
}
.text-heading {
  font-family: var(--font-display);
  font-size: var(--text-4xl);
  font-weight: var(--weight-bold);
  line-height: var(--leading-snug);
  letter-spacing: var(--tracking-snug);
}
.text-subheading {
  font-family: var(--font-display);
  font-size: var(--text-3xl);
  font-weight: var(--weight-bold);
  line-height: var(--leading-snug);
  letter-spacing: var(--tracking-normal);
}
.text-lead {
  font-size: var(--text-lg);
  font-weight: var(--weight-regular);
  line-height: var(--leading-relaxed);
  color: var(--color-text-muted);
}
.text-label {
  font-size: var(--text-xs);
  font-weight: var(--weight-semibold);
  letter-spacing: var(--tracking-wider);
  text-transform: uppercase;
}

Step 8: Layout utilities:
.container {
  width: 100%;
  max-width: var(--max-width);
  margin-inline: auto;
  padding-inline: var(--space-8);
}
@media (max-width: 768px) {
  .container { padding-inline: var(--space-5); }
  .text-hero { font-size: var(--text-4xl); }
  .text-display { font-size: var(--text-3xl); }
  .text-heading { font-size: var(--text-2xl); }
}
@media (max-width: 480px) {
  .container { padding-inline: var(--space-4); }
  .text-hero { font-size: var(--text-3xl); }
}

Step 9: Utility classes:
.sr-only {
  position: absolute; width: 1px; height: 1px;
  padding: 0; margin: -1px; overflow: hidden;
  clip: rect(0,0,0,0); white-space: nowrap; border: 0;
}
.truncate { overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.glass {
  background: rgba(255,255,255,0.7);
  backdrop-filter: blur(16px) saturate(180%);
  -webkit-backdrop-filter: blur(16px) saturate(180%);
  border: 1px solid rgba(255,255,255,0.5);
}
.glass-dark {
  background: rgba(13,13,13,0.7);
  backdrop-filter: blur(16px) saturate(180%);
  -webkit-backdrop-filter: blur(16px) saturate(180%);
  border: 1px solid rgba(255,255,255,0.08);
}

Step 10: Global keyframe animations:
@keyframes orb-float {
  0%, 100% { transform: translateY(0px) scale(1); }
  33% { transform: translateY(-14px) scale(1.025); }
  66% { transform: translateY(-8px) scale(1.015); }
}
@keyframes orb-breathe {
  0%, 100% { transform: scale(1); opacity: 0.9; }
  50% { transform: scale(1.07); opacity: 1; }
}
@keyframes orb-pulse-speak {
  0%, 100% { transform: scale(1); }
  25% { transform: scale(1.12); }
  50% { transform: scale(1.06); }
  75% { transform: scale(1.10); }
}
@keyframes particle-rise {
  0% { opacity: 1; transform: translateY(0) scale(1); }
  100% { opacity: 0; transform: translateY(-60px) scale(0.2); }
}
@keyframes fade-in {
  from { opacity: 0; transform: translateY(16px); }
  to { opacity: 1; transform: translateY(0); }
}
@keyframes marquee {
  from { transform: translateX(0); }
  to { transform: translateX(-50%); }
}
@keyframes shimmer {
  from { background-position: -400px 0; }
  to { background-position: 400px 0; }
}

Step 11: Reduced motion override:
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FILE 2: src/design-system/tokens.ts
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Export as TypeScript const objects (mirrors the CSS vars above exactly):
- colors object (bg, bgDark, text, textMuted, textSubtle, card, cardHover, border, borderStrong,
  surface, accent, accentLight, accentBg, accentHover, blue, blueLight, blueBg,
  success, warning, error)
- orbGradients object (medical, tourism, support, realEstate, travel, wellness, neutral, hero)
- spacing object (1 through 32, values as strings like '4px')
- radii object (sm, md, lg, xl, '2xl', '3xl', pill)
- shadows object (xs, sm, md, lg, xl, card, orbAccent, orbBlue, orbPurple, focus)
- typography object with fontDisplay, fontBody, fontMono, scale object
- buttons object with sizes (sm,md,lg,xl each having height/px/fontSize/fontWeight)
  and variants (primary, ghost, accent, subtle each having bg/text/hover/border)
- zIndex object (behind, base, raised, dropdown, sticky, overlay, modal, toast)
- animation object with duration and easing objects
Export a single default `tokens` object containing all above.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FILE 3: src/design-system/motion.ts
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Import Variants from framer-motion. Export these named constants:

pageVariants: Variants — initial: {opacity:0, y:20}, animate: {opacity:1, y:0, 
  transition:{duration:0.55, ease:[0.16,1,0.3,1]}}, exit: {opacity:0, y:-10, 
  transition:{duration:0.2}}

staggerContainer: Variants — animate: {transition:{staggerChildren:0.07, delayChildren:0.1}}

fadeUpItem: Variants — initial:{opacity:0, y:28}, animate:{opacity:1, y:0, 
  transition:{duration:0.5, ease:[0.16,1,0.3,1]}}

fadeInItem: Variants — initial:{opacity:0}, animate:{opacity:1, 
  transition:{duration:0.4, ease:'easeOut'}}

scaleItem: Variants — initial:{opacity:0, scale:0.92}, animate:{opacity:1, scale:1,
  transition:{duration:0.45, ease:[0.16,1,0.3,1]}}

orbFloat — animate:{y:[0,-18,0], scale:[1,1.03,1], 
  transition:{duration:5.5, repeat:Infinity, ease:'easeInOut'}}

orbListening — animate:{scale:[1,1.08,1], opacity:[0.9,1,0.9],
  transition:{duration:1.6, repeat:Infinity, ease:'easeInOut'}}

orbSpeaking — animate:{scale:[1,1.15,1,1.08,1],
  transition:{duration:0.75, repeat:Infinity, ease:'easeInOut'}}

orbIdle — animate:{scale:[1,1.02,1], opacity:[0.85,0.95,0.85],
  transition:{duration:4, repeat:Infinity, ease:'easeInOut'}}

cardHover — whileHover:{y:-5, boxShadow:'0 16px 48px rgba(0,0,0,0.13)',
  transition:{duration:0.2, ease:[0.16,1,0.3,1]}}

tabContent: Variants — initial:{opacity:0, x:16}, 
  animate:{opacity:1, x:0, transition:{duration:0.3, ease:[0.16,1,0.3,1]}},
  exit:{opacity:0, x:-16, transition:{duration:0.2}}

transcriptMessage: Variants — initial:{opacity:0, x:24, scale:0.94},
  animate:{opacity:1, x:0, scale:1, transition:{duration:0.3, ease:[0.16,1,0.3,1]}}

navbarReveal: Variants — initial:{y:-80, opacity:0},
  animate:{y:0, opacity:1, transition:{duration:0.5, ease:[0.16,1,0.3,1]}}

marqueeVariants — animate:{x:['0%','-50%'], 
  transition:{duration:28, repeat:Infinity, ease:'linear'}}

drawerSlide: Variants — initial:{x:'100%'}, animate:{x:0,
  transition:{duration:0.4, ease:[0.16,1,0.3,1]}},
  exit:{x:'100%', transition:{duration:0.3}}

modalScale: Variants — initial:{opacity:0, scale:0.94, y:12},
  animate:{opacity:1, scale:1, y:0, transition:{duration:0.35, ease:[0.16,1,0.3,1]}},
  exit:{opacity:0, scale:0.96, y:8, transition:{duration:0.2}}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FILE 4: src/design-system/breakpoints.ts
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Export:
export const breakpoints = {
  xs: 480, sm: 640, md: 768, lg: 1024, xl: 1280, '2xl': 1536
} as const;
export type Breakpoint = keyof typeof breakpoints;

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FILE 5: src/design-system/index.ts
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Re-export everything:
export * from './tokens';
export * from './motion';
export * from './breakpoints';

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FILE 6: src/utils/cn.ts
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

import { clsx, type ClassValue } from 'clsx';
import { twMerge } from 'tailwind-merge';
export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FILE 7: src/utils/formatters.ts
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Export these functions:
- formatNumber(n: number): string — compact format: 1200 → '1.2K', 1000000 → '1M'
- formatDuration(ms: number): string — 90000 → '1:30', 3661000 → '1:01:01'
- formatLatency(ms: number): string — 1800 → '1.8s', 800 → '800ms'
- truncateText(text: string, maxLength: number): string
- capitalizeFirst(str: string): string
- slugify(str: string): string — 'Medical Agent' → 'medical-agent'
- getInitials(name: string): string — 'Dr. Aarav Singh' → 'DA'

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FILE 8: src/hooks/useMediaQuery.ts
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Custom hook: useMediaQuery(query: string): boolean
- Uses window.matchMedia
- Adds/removes event listener on mount/unmount
- SSR-safe (returns false if window undefined)

Also export convenience hooks:
- useIsMobile(): boolean — (max-width: 768px)
- useIsTablet(): boolean — (max-width: 1024px)
- useIsDesktop(): boolean — (min-width: 1025px)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FILE 9: src/hooks/useScrollAnimation.ts
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Custom hook: useScrollAnimation(options?: IntersectionObserverInit)
Returns: { ref: RefObject<HTMLElement>, isVisible: boolean, hasAnimated: boolean }
- Uses IntersectionObserver
- Default threshold: 0.12, rootMargin: '0px 0px -48px 0px'
- Once visible, hasAnimated stays true (one-shot animation)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FILE 10: src/router/index.tsx
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Export:
export const ROUTES = {
  HOME: '/',
  AGENTS: '/agents',
  AGENT_TALK: (id: string) => `/agents/${id}/talk`,
  VOICES: '/voices',
  PRICING: '/pricing',
  DOCS: '/docs',
  LOGIN: '/login',
  SIGNUP: '/signup',
  DASHBOARD: '/app/home',
  MY_AGENTS: '/app/agents',
  ANALYTICS: '/app/analytics',
  SETTINGS: '/app/settings',
} as const;

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FILE 11: src/main.tsx
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

import React from 'react';
import ReactDOM from 'react-dom/client';
import { BrowserRouter } from 'react-router-dom';
import App from './App';
import './styles/globals.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <BrowserRouter>
      <App />
    </BrowserRouter>
  </React.StrictMode>
);

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FILE 12: src/App.tsx
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- Import React, lazy, Suspense
- Import Routes, Route, useLocation from react-router-dom
- Import AnimatePresence from framer-motion

Lazy-load every page:
const LandingPage = lazy(() => import('./pages/LandingPage'));
const AgentExplorerPage = lazy(() => import('./pages/AgentExplorerPage'));
const AgentConversationPage = lazy(() => import('./pages/AgentConversationPage'));
const VoicesPage = lazy(() => import('./pages/VoicesPage'));
const PricingPage = lazy(() => import('./pages/PricingPage'));
const LoginPage = lazy(() => import('./pages/AuthPage/LoginPage'));
const SignupPage = lazy(() => import('./pages/AuthPage/SignupPage'));
const DashboardPage = lazy(() => import('./pages/DashboardPage'));
const NotFoundPage = lazy(() => import('./pages/NotFoundPage'));

Fallback component — LoadingScreen:
A centered full-screen div with background var(--color-bg).
Inside: a 56px × 56px div with background var(--orb-hero), 
border-radius 50%, animation orb-breathe 1.6s ease-in-out infinite.
Below it: "Bhasini" text in var(--font-display), 14px, var(--color-text-muted), 
margin-top 20px, letter-spacing 0.08em.

App component:
- const location = useLocation()
- Return:
  <AnimatePresence mode="wait" initial={false}>
    <Suspense fallback={<LoadingScreen />}>
      <Routes location={location} key={location.pathname}>
        <Route path="/" element={<LandingPage />} />
        <Route path="/agents" element={<AgentExplorerPage />} />
        <Route path="/agents/:agentId/talk" element={<AgentConversationPage />} />
        <Route path="/voices" element={<VoicesPage />} />
        <Route path="/pricing" element={<PricingPage />} />
        <Route path="/login" element={<LoginPage />} />
        <Route path="/signup" element={<SignupPage />} />
        <Route path="/app/home" element={<DashboardPage />} />
        <Route path="*" element={<NotFoundPage />} />
      </Routes>
    </Suspense>
  </AnimatePresence>

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STUB ALL PAGE FILES (so imports don't break):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Create minimal stub for each lazy-loaded page — each file exports a default 
React component that returns a single <div> with the page name as text.
Files to stub:
- src/pages/LandingPage/index.tsx
- src/pages/AgentExplorerPage/index.tsx
- src/pages/AgentConversationPage/index.tsx
- src/pages/VoicesPage/index.tsx
- src/pages/PricingPage/index.tsx
- src/pages/AuthPage/LoginPage.tsx
- src/pages/AuthPage/SignupPage.tsx
- src/pages/DashboardPage/index.tsx
- src/pages/NotFoundPage/index.tsx

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FINAL STEP: Run these commands in sequence:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

npm install
npm run dev

Confirm: zero TypeScript errors, dev server starts, 
LoadingScreen shows briefly then LandingPage stub loads.
Report back the exact terminal output.